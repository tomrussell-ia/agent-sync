{
  "name": "agent-sync-prettier",
  "version": "1.0.0",
  "private": true,
  "description": "Prettier configuration for Agent Sync markdown files",
  "devDependencies": {
    "prettier": "^3.4.2"
  },
  "scripts": {
    "build": "echo 'No build needed for prettier config'",
    "test": "prettier --check \"**/*.md\"",
    "format": "prettier --write \"**/*.md\"",
    "format:check": "prettier --check \"**/*.md\""
  }
}
